version: "3.1"

services:
  database:
    image: mariadb
    restart: always
    environment:
      MYSQL_DATABASE: devpleno
      MYSQL_ROOT_PASSWORD: admin
    ports:
      - 3306:3306

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
